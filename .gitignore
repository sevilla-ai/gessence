# Ada build artifacts
*.o
*.ali
*.exe
b__*
*.bexch

# GPR build directories
obj/
obj-tests/
obj-native/
obj-wasm/
.objs/

# Ada compiled binaries (keep source, not binaries)
# Root anchored, so tests/main_essence_test.adb stays tracked.
/hello
/main_*
/test_essence_resolver

# Generated x86 assembly listings (make asm)
asm/

# WebAssembly output (generated, not source)
*.wasm
*.js

# Private infrastructure — configure locally, never commit
# Copy world.example → world and fill in your own values
container/world
container/ada-dev.sh

# Signal files (runtime, not source)
.ada-signal

# Editor artifacts
.vscode/settings.json
*.gpr.bak
default.gpr

# OS artifacts
.DS_Store
Thumbs.db
